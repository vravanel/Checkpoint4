{% extends 'admin/base.html.twig' %}

{% block title %}Horse{% endblock %}

{% block body %}
    <h1>Horse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ horse.name }}</td>
            </tr>
            <tr>
                <th>Breed</th>
                <td>{{ horse.breed }}</td>
            </tr>
            <tr>
                <th>Personality</th>
                <td>{{ horse.personality }}</td>
            </tr>
            <tr>
                <th>Skill</th>
                <td>{{ horse.skill }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ horse.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_horse_index') }}">back to list</a>

    <a href="{{ path('app_admin_horse_edit', {'id': horse.id}) }}">edit</a>

    {{ include('admin/admin_horse/_delete_form.html.twig') }}
{% endblock %}
