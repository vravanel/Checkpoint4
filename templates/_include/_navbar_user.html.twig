
        <div>
            <ul class="nav flex-column nav-aside">
                {% set links = [
                    {'title': 'Accueil',
                        'path': 'app_user',
                    },
                    {'title': 'Informations personnelles',
                        'path': 'app_account_information',
                    },
                    {'title': 'Rendez-vous',
                        'path': 'app_appointment',
                    },
                    {'title': 'Retour sur le site',
                        'path': 'app_home',
                    },
                    {'title': 'Déconnexion',
                        'path': 'app_logout',
                    },
                ] %}

                {% for link in links %}
                    <li class="nav-item">
                        <a href="{{ path(link.path) }}" class="nav-link text-white
                {% if app.request.attributes.get('_route') == link.path %}actif{% endif %}"
                           {% if app.request.attributes.get('_route') == link.path %}aria-current="page"{% endif %}>
                            {{ link.title }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
