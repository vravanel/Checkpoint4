<nav class="navbar navbar-expand-lg mt-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active text-secondary" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('app_equitherapie') }}">Equithérapie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="#">Structure</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="#">Cavalerie</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('app_user') }}">Mon espace</a>
                </li>
                <li class="nav-item">
                        <a class="nav-link text-secondary" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('app_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('app_login') }}">Connexion</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>